import React from "react";
import { Landing } from "../src/views";
import { NavBar } from "../src/components";

const home = () => {
  return (
    <div>
      <NavBar />
      <Landing />
    </div>
  );
};

export default home;
